/*
Task 1: Calculating the Square of Numbers
Using arrow functions with array methods like map().
*/

const numbers = [1, 2, 3, 4, 5];
const squareNumbers = numbers.map(number => number ** 2);

console.log(squareNumbers);