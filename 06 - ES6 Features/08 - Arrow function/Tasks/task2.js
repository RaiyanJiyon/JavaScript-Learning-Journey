/*
Task 2: Greeting Users with forEach()
Using arrow functions with forEach() to greet each user in an array.
*/

const users = ['Alice', 'Bob', 'Charlie'];
users.forEach(user => console.log(`Welcome ${user}`));